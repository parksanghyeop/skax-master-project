"""core 계층 — 에이전트의 언어 무관 핵심 로직.

이 층은 대상 프로젝트가 어떤 언어·빌드 도구를 쓰는지 모른다(절대 규칙 R1).
특정 언어·도구 이름 문자열이 이 디렉터리에 등장하면 tests/test_layering.py가 실패한다.
구체 구현(실제 빌드 도구 호출, 파일 파싱)은 전부 adapters/에 있고,
core는 ports.py의 인터페이스를 통해서만 바깥과 만난다.
"""
