"""파이프라인 계층 — 변경 추출 → 의도 분류 → 조치 결정 (M5, v4 2.1).

다섯 단계 중 앞의 세 단계가 여기 산다(테스트 작성은 writer_graph, 품질 검사는
게이트). LLM이 등장하는 곳은 의도 분류 하나뿐이고(R2), 조치 결정은 규칙표
(일반 코드)다 — 길이 틀리면 사고, 지침서가 틀리면 품질 저하일 뿐이라는
피해 비대칭이 이 분리의 이유다(v4 2.1 Step 2).
층: core — 대상 언어를 모른다(R1). 변경 추출의 실물 구현은 adapters/ 밑에 있다.
"""
