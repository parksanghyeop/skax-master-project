"""cli 계층 — 사용자가 터미널에서 쓰는 명령(cta)의 조립과 진입점 (v4 6.1의 cli/).

핵심 규칙(v4 Step 3): 에이전트가 만든 테스트는 **즉시 반영되지 않는다** —
생성 결과는 제안(proposal)으로 보관되고, 사용자가 `cta diff`로 검토한 뒤
`cta apply`로만 테스트 트리에 들어간다. 이 층은 조립·입출력만 하고
판단 로직은 core/adapters/graph/llm에 있다.
"""
