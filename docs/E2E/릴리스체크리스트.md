# 릴리스 체크리스트 — 3단계 마지막 주

"될 것 같다"는 미완이다. 각 항목 옆에 근거(명령 결과·파일·커밋)를 적고 체크한다.
phase3 스킬의 체크리스트 5항목을 포함하고, 이 프로젝트의 규칙(R1~R7, DoD)을 더했다.

## 자동 검사

- [x] `ruff check . && ruff format --check .` 통과 — 근거: 2026-09-06 로컬(py3.12 venv) All checks passed, 152 files formatted
- [x] `pytest -q` 전체 통과(재생 모드, 실호출 0) — 근거: 2026-09-06 222 passed, 4 deselected (e2e-notes 검증 기록)
- [ ] `pytest -m docker` 통과(게이트 불변식·재생 통합) — 근거:
- [ ] `pytest -m neo4j` 통과 — 근거:
- [x] `tests/test_layering.py` 통과 — core에 언어 문자열 없음 — 근거: pytest -q 포함(core/config.py 주석이 걸려 고친 이력 — e2e-notes)
- [x] CI `check` 잡 그린(최근 커밋) — 근거: GitHub Actions `ci` success @ 50fa679(B-5), 6f63f91(B-4 — setup-java + check_defects 포함, Linux에서 12/12) (2026-09-06)
- [ ] 저장된 LLM 호출 기록을 하나 지운 브랜치에서 CI가 빨강(폴백 없음 증명) — 근거:
- [x] 시크릿 테스트 3건 초록(환경변수만 / 샌드박스 미전달 / 마스킹) — 근거: tests/test_llm_config.py + tests/test_secrets.py(8건)
- [x] 커밋 전 diff에 `atl-`·게이트웨이 주소·키 모양 문자열 없음 — 근거: 2026-09-06 커밋 전 grep(테스트용 가짜 키 atl-test-key·atl-supersecret만)

## 동작

- [ ] 데모 ① 콜드 스타트 3회 연속 성공 — 근거(`docs/E2E/데모실행기록.md`):
- [ ] 데모 ② 유지보수(버그 수정 → 재발 방지 / 리팩터링 깨짐 → escalate → resolve) 3회 연속 성공 — 근거:
- [ ] 종료 코드 0 / 2 / 3 / 1 각각 재현 — 근거:
- [x] `cta.toml` 한 파일로 게이트·재시도·시간 초과·모델 변경 확인 — 근거: tests/test_config.py(6건: 절 5개 읽기·우선순위·상한 검증)
- [ ] MCP 서버: Claude Code에서 `generate`·`maintain` 호출 성공 — 근거(캡처):
- [ ] CLI와 MCP가 같은 core 조립 함수를 호출(로직 중복 없음) — 근거(grep):
- [ ] 스킬 전/후 표 — 하네스 결과 파일과 함께 — 근거(`evals/results/`):
- [ ] 결함 세트 v2(12건 이상) 검출률 기록 — 근거: 세트는 12건 완료 + 자기 검사 12/12(`scripts/check_defects.py`). `cta eval` 실측은 Docker·게이트웨이 환경에서
- [ ] Linux에서 `pip install` → `cta demo` 1회 성공 — 근거:
- [ ] 설치 테스트: 리포를 모르는 사람 1명이 사용가이드만으로 `cta generate` 성공 — 근거:

## 문서

- [x] 루트 `README.md`가 제품 README(소개·준비물·5분 시작·명령 요약·문서 지도·한계) — 근거: 2026-09-06 재작성, 구현 킷 내용은 docs/개발환경.md로
- [x] `docs/사용가이드.md`에 "지원 범위와 한계" 절, `PoC구현.md` §5와 1:1 — 근거: 사용가이드 §15(8행) ↔ PoC구현 §5 제한적 동작 표
- [x] ADR 목록 = 구현. 0001~0009 반입 또는 한 줄 요약. 신규 ADR(스킬·MCP·압축 불필요·설정) 존재 — 근거: docs/adr/README.md 색인(0009 요약, 0016~0018 신규). 0001~0008 원문은 미반입 표기
- [x] `docs/architecture.md` 모듈 표에 새 모듈 전부(config·skills·mcp) — 근거: config·masking·hints·skills·mcp·check_defects·ci.yml 행 추가
- [x] `docs/contracts.md` ↔ `PoC구현.md` §2.0 필드명 일치 — 근거: 2026-09-06 코드 대조(WriterState·ChangedSymbol·Intent·ActionDecision) + 설정·스킬·MCP 절 추가
- [x] CLAUDE.md의 LLM 백엔드 표기가 실제(gpt-5 단일 deployment)와 일치 — 근거: 2026-09-06 "Kimi/Qwen/GLM" → "Azure OpenAI 호환, gpt-5(ADR-0011·0013)"
- [ ] 산출물 `docs/제출자료/E2E서비스개발.md` — 한눈에 보기 10줄 + 실행 로그 원문 + 전/후 표 + 3구역 — 근거: 초안 작성(한눈에 보기·계약·구현·문제 해결·검증 로그·3구역). 전/후 표는 [측정 대기]
- [x] `PoC구현.md` §3 문제 해결 표를 `핵심구현.md`로 이동(길이 조정) — 근거: 2026-09-06 PoC구현 §3 대표 3행, 핵심구현 §9 전체 12행
- [ ] 하네스 최종 수치를 발표 자료 형식으로 export — 근거:

## 마지막

- [ ] 데모 고정 이후 게이트 통과 없는 변경이 없었는지 커밋 로그 확인 — 근거:
- [ ] `/done-check` 4줄 보고(변경·검증·미완·다음) 작성 — 근거:
- [ ] 데모 녹화 — 근거(파일):
