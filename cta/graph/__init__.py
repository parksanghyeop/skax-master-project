"""graph 계층 — 코드 그래프의 모델·저장소·질의 응답 (M4, v4 4.1).

노드 3종(클래스·메서드·테스트)과 확정 엣지 3종(선언한다 DECLARES, 만든다 CREATES,
검증한다 COVERS — 커버리지 실측)을 담는다. 추정이 필요한 "호출한다 CALLS"는 후순위.
언어 지식은 없다 — 그래프를 채우는 쪽(adapters/java의 빌더)이 언어를 안다.
저장소는 Neo4j(실물)와 인메모리(테스트·소규모 폴백) 두 구현이 같은 인터페이스를 따른다.
"""
