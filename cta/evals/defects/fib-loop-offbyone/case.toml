# 결함 케이스: 반복 경계 off-by-one — n>=2에서 한 항 앞의 값을 돌려준다
target = "MathUtil#fibonacci"
class_rel = "src/main/java/com/example/bench/MathUtil.java"
bug = 'i <= n을 i < n으로 — fibonacci(5)가 3'
# 자기 검사(scripts/check_defects.py): 고친 버전은 expected, 버그 버전은 그와 다른 값이어야 한다
probe = 'new MathUtil().fibonacci(5)'
expected = "5"
