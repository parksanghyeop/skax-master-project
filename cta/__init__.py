"""cta — Code Test Agent 제품 코드 루트 패키지.

층 구조는 이 아래에 그대로 있다: core(언어 무관 로직) / adapters(Java·Maven) /
llm(게이트웨이·기록 재생) / graph(코드 그래프) / sandbox(Docker) / cli(명령) /
evals(평가 하네스·데이터). 의존 방향은 docs/architecture.md 참조.
"""
