# Code Test Agent — CLAUDE.md

Java 소스 변경에 맞춰 테스트 코드를 자동 생성·유지보수하는 LLM 에이전트.
Python + LangGraph, 사내 LLM 게이트웨이(Azure OpenAI 호환, 기본 deployment gpt-5 — ADR-0011·0013), Neo4j 코드 그래프, Docker 샌드박스.

## 진실의 원천

설계 문서가 코드보다 우선한다. 충돌하면 코드를 고치기 전에 사용자에게 알린다.

- `docs/contracts.md` — 데이터 모델·도구 시그니처. **모든 작업 전 확인**
- `docs/architecture.md`, `docs/adr/` — 구조·결정 기록. ADR을 위반하는 코드를 쓰지 않는다
- `02_상세설계_및_개발환경구축_v4.md` — 최신 확정 설계. **구버전 문서와 충돌 시 v4가 이긴다**

v4에서 바뀐 결정 (구문서·구스킬과 다름):
- 도구 `delegate_exploration` 폐기 → `query_code_graph` (ADR-0009)
- 테스트 작성 루프: 직접 while → **LangGraph 서브그래프 + interrupt(사용자 개입)**
- 조치 결정 = 두 층: 경로(규칙표) + 작업 지침서(LLM 구체 분석)
- 품질 게이트에 커버리지 기준치 추가: 변경 라인 80%/분기 70%, 회귀 금지

## 절대 규칙

- **R1. `core/`는 언어를 모른다** — `java`, `maven`, `pom.xml`, `junit`, `jacoco`, `mvn`, `pitest` 문자열 금지. `tests/test_layering.py`가 검사하며, 이 테스트를 예외 처리로 우회하지 않는다
- **R2. 안전장치는 결정적** — 조치 결정 경로표와 품질 게이트에 LLM 호출 금지. LLM이 있는 곳은 의도 분류·작업 지침서·테스트 작성뿐
- **R3. refactor + 테스트 실패 → escalate** — 기대값을 갱신하는 분기를 만들지 않는다. 의도 분류가 불확실하면 추측하지 않고 사용자에게 묻는다
- **R4. 도구는 정확히 6개** — `inspect_target`, `query_code_graph`, `write_test`, `run_tests`, `check_quality`, `report_finding`. 7번째가 필요하면 먼저 사용자에게 묻는다
- **R5. 전체 테스트 실행 금지** — 어댑터가 빈 selector를 거부한다
- **R6. 샌드박스 밖에서 대상 코드 실행 금지** — 기본값은 Docker 샌드박스. 예외는 사용자가 `--fast` 또는 `--runner local`로 **명시한 로컬 실행 모드**뿐이다(ADR-0019, 화면 경고 필수). 코드가 스스로 로컬로 폴백하지 않는다. 디버깅 목적의 임의 실행도 금지
- **R7. LLM 호출은 `llm/` 계층만 경유** — record & replay가 작동해야 한다. CI는 replay 모드, 카세트 없으면 실패(실호출 폴백 금지)

## LLM 백엔드·시크릿 (ADR-0011)

- **백엔드는 사내 게이트웨이 하나다**(Azure OpenAI 호환, deployment가 곧 모델).
  클라이언트 생성은 `llm/config.py`의 `make_llm_client()`만 경유한다 —
  직접 생성 금지. 스펙이 바뀌면 llm/ 계층만 고친다
- **서버 주소·API 키는 환경변수 또는 `.env`(gitignore 대상)로만** 전달한다(v4 6.6).
  코드, 커밋되는 설정 파일, 카세트, 로그에 남기는 순간 사고다.
  커밋 전 diff에서 `atl-`, 주소, 키 모양 문자열을 확인한다
- 커밋 가능한 설정 문서는 `.env.example`(키 이름만, 값 없음)뿐이다
- deployment(모델)를 바꾸면 골든 카세트를 재녹음한다(`scripts/record_golden.py --live`) —
  모델 이름이 재생 대조 키에 포함되기 때문

## 명령

```bash
ruff check . && ruff format --check .   # 린트·포맷
pytest -q                               # 단위 테스트 (test_layering.py 포함)
```

## Definition of Done — 전부 만족해야 "됐다"

1. ruff 통과  2. pytest 통과  3. 새 동작에 테스트 존재(Fake 어댑터 또는 카세트)
4. 공개 함수 타입 힌트  5. 실제 실행해 봤다("될 것 같다" 금지)  6. 설계 문서와 일치
7. 주석 규칙 충족(아래)  8. 관련 문서 같은 커밋에서 갱신(아래 표)

## 카파시 4원칙 (Andrej Karpathy의 LLM 코딩 지침)

모든 코드 작업에 적용한다. 사소한 수정(오타, 명백한 한 줄)에는 격식을 생략해도 된다.

1. **Think Before Coding — 코딩 전에 생각한다**
   가정을 명시하고, 모호하면 추측하지 말고 묻는다. 요청이 이상하면 반박한다.
   헷갈리는 지점은 이름 붙여 드러낸다 — 혼란을 추측으로 해소하지 않는다
2. **Simplicity First — 단순함 먼저**
   문제를 푸는 최소한의 코드만. 추측성 기능·1회용 추상화·요청 없는 기능 금지.
   "시니어가 보면 과하다고 할까?"가 판정 기준
3. **Surgical Changes — 수술적 변경**
   바뀐 모든 줄이 요청으로 소급돼야 한다. 옆 코드·주석·포맷을 "개선"하지 않는다.
   내 변경이 만든 고아(안 쓰는 import 등)는 치우고, 원래 있던 고아는 보고만 한다
4. **Goal-Driven Execution — 검증 가능한 목표로 실행**
   모호한 요청을 검증 가능한 기준으로 바꾼 뒤 시작한다: "버그 고쳐" → "재현
   테스트를 먼저 쓰고, 통과시킨다". 다단계 작업은 단계마다 → 검증: [확인 방법]을 붙인다

## 주석 규칙 — 모든 소스 코드에 적용

독자는 **이 코드를 처음 보는 2년차 개발자**다. 그 사람이 파일만 읽고 따라올 수 있어야 한다.

- **파일 머리**: 이 파일의 책임 1~3줄 + 어느 층(core/adapters/llm/...)에 왜 있는지
- **공개 함수·클래스 docstring**: 무엇을 하나 / 왜 필요한가 / 입력·출력 / 실패 시 동작
- **"왜" 주석**: 코드가 말하지 못하는 것 — 설계 이유, 함정 회피, 대안을 버린 이유.
  안전장치 코드(경로표·품질 게이트)에는 "왜 LLM을 안 쓰는지" + 관련 ADR 번호를 반드시 단다
- **매직값 금지**: 숫자·문자열 상수에는 이름과 출처 주석 (예: `COVERAGE_LINE_MIN = 0.80  # v4 설계 2.4, 설정으로 조정 가능`)
- **까다로운 블록**: 3단계 이상 논리나 비직관적 코드 앞에는 흐름을 한글 한 줄로 요약
- **TODO**: 반드시 근거 연결 — `# TODO(2단계): ... poc-findings '2단계 반영' 3번`

## 문서 병행 규칙 — 코드와 같은 커밋에서

"나중에 문서화"는 없다. 무엇을 바꾸면 무엇을 갱신하는지:

| 바꾼 것 | 같이 갱신할 문서 |
|---|---|
| 데이터 모델·도구 시그니처 | `docs/contracts.md` |
| 구조·의존 방향 | `docs/architecture.md` |
| 설계 결정 변경 | `docs/adr/` 새 번호 (코드보다 먼저) |
| CLI 명령·설정값 | `README.md` 사용법 |
| 새 모듈 | 모듈 docstring + `docs/architecture.md`의 모듈 표 한 줄 |
| PoC 중 발견·해결 | `docs/poc-findings.md` (리서치 출처 포함, 그 자리에서) |

## 글쓰기 가이드라인 — 주석·문서·산출물 공통

- **독자 기준**: 2년차 주니어. 그 연차가 이미 아는 실무 용어는 그대로 쓴다
  (git diff, 커밋, assert, 커버리지, 샌드박스, 린트, 프롬프트, LLM, 스킵)
- **어려운 용어는 실무 명칭 그대로 + 첫 등장에 한 줄 풀이**
  (예: "구문 트리(AST — 코드를 구조로 파싱한 것) 비교")
- **조어 금지**: 실무에서 안 쓰는 번역어를 만들지 않는다.
  ❌ 단언 약화 검사 → ⭕ assert 검사 / ❌ 격리 컨테이너 → ⭕ Docker 샌드박스 /
  ❌ 접속 열쇠 → ⭕ API 토큰 / ❌ 코드 관계 지도 → ⭕ 코드 그래프
- **좁은 은어도 금지**(사용자 피드백, 2026-09-01): 특정 라이브러리 문화권에서만 통하는
  비유어는 산출물·사용자 대면 문서에 쓰지 않는다.
  ❌ 카세트 → ⭕ 저장된 LLM 호출 기록(파일) / ❌ 골든 케이스 → ⭕ 대표 검증 시나리오 /
  ❌ 재녹음 → ⭕ 기록 재생성. 코드 식별자(cassette 등)는 유지해도 된다
- **문장은 짧게, 목록은 "핵심어: 설명" 한 줄**, 산출물 서술은 "~다" 문체
- **예시 하나가 설명 열 줄보다 낫다** — 개념을 적으면 입·출력 예를 붙인다

## 작업 방식

- 얇은 수직 슬라이스: 한 케이스가 끝에서 끝까지 도는 것부터
- 파일 400줄 초과 시 분할 검토. 도구·파이프라인 단계는 1개당 1파일
- 새 의존성은 추가 전에 묻는다. 커밋은 논리 단위로 작게
- 설계가 바뀌는 결정을 했다면 코드보다 ADR을 먼저 쓴다

## 단계별 스킬

현재 단계에 맞는 스킬을 따른다. 단계 전환은 관문 통과 + 사용자 승인으로만.

| 단계 | 스킬 | 요약 |
|---|---|---|
| 1. PoC | `.claude/skills/phase1-poc` | 수직 슬라이스 1개 + 문제점 리포트 |
| 2. 테스트·고도화 | `.claude/skills/phase2-hardening` | 파이프라인·게이트·평가 하네스 |
| 3. E2E 서비스 | `.claude/skills/phase3-e2e` | CLI·MCP·장기기억·릴리스 |

## 보고 형식

```
변경:  무엇을 만들었는가
검증:  어떤 명령으로 확인했는가 (실제 실행 결과)
미완:  남은 것 / 막힌 것
다음:  제안하는 다음 단계
```
